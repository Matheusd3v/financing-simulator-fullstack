name: Frontend CD

on:
  push:
    paths:
      - 'frontend/**'
      - '!backend/**'
    branches:
      - main      

jobs:
  docker-build-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download frontend build
        uses: actions/download-artifact@v4
        with:
          name: frontend-build
          path: frontend/build

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: matheusgomesa/financing-simulator-front:${{ github.run_id }}
          build-args: |
            VITE_API_URL=https://simulador-fin-api.matheus-backend-dev.tech/api

  # deploy:
  #   runs-on: [self-hosted, k8s]
  #   needs: build-and-push
  #   if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' || github.event_name == 'pull_request' && github.event.pull_request.merged == true }}
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4

  #     - name: Deploy Frontend (update image)
  #       run: |
  #         kubectl set image deployment/frontend-deployment \
  #           frontend=matheusgomesa/financing-simulator-front:${{ github.run_id }} \
  #           -n simulador-fin
  #         kubectl rollout status deployment/frontend-deployment -n simulador-fin
